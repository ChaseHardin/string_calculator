# String Calculator Kata
*Completed using JavaScript ES6, Mocha, and Chai*

# Setup Development Environment
* To install dependencies, run `npm install`
* To execute all the unit tests, run `npm test`
